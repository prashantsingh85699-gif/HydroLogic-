# ── Required ──
GEMINI_API_KEY=your-gemini-api-key-here

# ── Optional (system works without these — falls back to log-only) ──
SENDGRID_API_KEY=
SLACK_WEBHOOK_URL=
NOTIFICATION_EMAIL=vendor@utility.com
